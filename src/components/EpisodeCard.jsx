import React from 'react'

function EpisodeCard({imageProp, episodeProp}) {

    // const { image, episodeValue } = props

    return (
        <div>

        </div>
    )
}

export default EpisodeCard
